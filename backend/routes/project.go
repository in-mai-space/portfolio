package route

import "github.com/gofiber/fiber/v2"

func ProjectRoute(app fiber.Router) {
	// app.Get("/projects")

	// app.Get("/projects/:id")

	// app.Post("/projects")

	// app.Put("/projects/:id")

	// app.Delete("/projects/:id")
}
