package route

import "github.com/gofiber/fiber/v2"

func SocialRoute(app fiber.Router) {
	// app.Get("/socials")

	// app.Post("/socials")

	// app.Put("/socials/:id")

	// app.Delete("/socials/:id")
}
