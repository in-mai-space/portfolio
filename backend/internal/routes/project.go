package route

import "github.com/gofiber/fiber/v2"

func ProjectRoute(app fiber.Router) {
	route := app.Group("/projects")

	// app.Get("/")

	// app.Get("/:id")

	// app.Post("/")

	// app.Put("/:id")

	// app.Delete("/:id")
}
