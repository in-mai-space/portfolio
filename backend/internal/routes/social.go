package route

import "github.com/gofiber/fiber/v2"

func SocialRoute(app fiber.Router) {
	route := app.Group("/socials")

	// app.Get("/")

	// app.Post("/")

	// app.Put("/:id")

	// app.Delete("/:id")
}
